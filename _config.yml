# Jekyll Configuration
title: Kevin Patrick Westropp
description: Senior Fullstack Engineer — React, Node.js, TypeScript, AWS
url: "https://highlanderkev.github.io"
baseurl: ""

# Disable the minima theme so index.html is served as-is
# theme: minima  <-- commented out intentionally

plugins:
  - jekyll-seo-tag
  - jekyll-feed

# Exclude files from build
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
